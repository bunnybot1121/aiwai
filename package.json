{
  "name": "saveflow-ai-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build"
  },
  "dependencies": {
    "@designcodeio/threeui": "^1.1.0"
  }
}
